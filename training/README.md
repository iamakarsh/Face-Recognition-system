# Training

Keep Colab or training-only code here so local inference stays separate from model training.

Do not hardcode Roboflow credentials in notebooks or scripts. Use environment variables such as `ROBOFLOW_API_KEY`.

The current local app expects the trained YOLOv8 weights at:

```text
models/best.pt
```
